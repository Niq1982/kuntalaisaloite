#-----------------------------------------------------------------------------
# the following params are only for testing (remove them in production)
#-----------------------------------------------------------------------------

# sends all emails to this address instead of real addresses
test.email.send-to=pauli.karpanoja@solita.fitest

# outputs emails to console instead of sending them
test.email.consoleOutput=false

# See ReloadableResourceBundleMessageSource.setCacheSeconds
test.messageSourceCacheSeconds=1

# This is resource direction for images for OM users while editing help info pages.
om.image.directory=/Users/paulika/Documents/work/Aloitepalvelu/all/om-images

test.email.municipality.emails.to.author=false
test.email.send.moderator.emails.to.author=false