<#-- Use ONLY inline CSS-styles in HTML emails  -->

<#assign bodyBGcolor = "#f0f0f0" />
<#assign blockBGcolor = "#ffffff" />
<#assign defaultFontSize = "14px" />
<#assign defaultFontFamily = "'PT Sans','Trebuchet MS',Helvetica,sans-serif" />

<#assign defaultFont = "font-size:${defaultFontSize}; font-family:${defaultFontFamily};" />

<#assign bigTitle = "font-size:20px; margin:0; color:#087480; font-weight:normal; font-family:${defaultFontFamily}" />
<#assign h4 = "font-size:${defaultFontSize}; margin:1em 0 0 0;" />

<#assign smallFont = "font-size:12px;" />

<#assign pTopMargin = "font-size:${defaultFontSize}; margin:1em 0 0 0;" />
<#assign pBottomMargin = "font-size:${defaultFontSize}; margin:0 0 1em 0;" />
<#assign pBothMargins = "font-size:${defaultFontSize}; margin:1em 0;" />

<#assign footerFont = "margin:0; color:#686868; font-size:12px;" />
